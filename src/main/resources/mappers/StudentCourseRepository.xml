<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="raisetech.student.mapper.StudentCourseMapper">

    <!-- マップの定義 -->
    <resultMap id="StudentCourseResultMap" type="raisetech.student.data.StudentCourse">
        <id column="id" property="id" />
        <result column="student_id" property="studentId" />
        <result column="course_name" property="courseName" />
        <result column="course_start_at" property="courseStartAt" />
        <result column="course_end_at" property="courseEndAt" />
    </resultMap>

    <!-- SQL の定義例 -->
    <select id="findAll" resultMap="StudentCourseResultMap">
        SELECT id, student_id, course_name, course_start_at, course_end_at
        FROM student_course
    </select>

</mapper>